<form method="post" action="{{ path('avancee_delete', {'id': avancee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avancee.id) }}">
    <button class="btn">Delete</button>
</form>
