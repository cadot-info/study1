{% extends 'admin/index.html.twig' %}

{% block title %}Avancee{% endblock %}

{% block body %}
<h1>Avancee</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ avancee.id }}</td>
        </tr>
        <tr>
            <th>Titre</th>
            <td>{{ avancee.Titre }}</td>
        </tr>
        <tr>
            <th>Texte</th>
            <td>{{ avancee.Texte|raw }}</td>
        </tr>
    </tbody>
</table>
<div class="row">
    <a class="btn  btn-primary mr-2" href="{{ path('avancee_index') }}">Revenir à la liste</a>

    <a class="btn  btn-secondary mr-2" href="{{ path('avancee_edit', {'id': avancee.id}) }}">Editer</a>

    {{ include('avancee/_delete_form.html.twig') }}
</div>
{% endblock %}