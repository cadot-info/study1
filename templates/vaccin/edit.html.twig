{% extends 'admin/index.html.twig' %}

{% block title %}Editer les actualités sur le Vaccin{% endblock %}

{% block body %}
<h1>Editer l'actualité</h1>
<div class="row">
    <div class="col-12">
        {{ include('vaccin/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a class="btn btn-primary" href="{{ path('vaccin_index') }}">Revenir à la liste</a>

        {{ include('vaccin/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}